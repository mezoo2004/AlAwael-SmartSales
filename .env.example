# Supabase (required for remote lead persistence)
# Create a project at https://supabase.com and paste values from Project Settings → API
VITE_SUPABASE_URL=https://izjtiwpoxqntusjeirgk.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_STpFX8c5ArWDU4ZWn-X9Gg_FTSikk3c

# Never put the service_role key in frontend env files.

# ---------------------------------------------------------------------------
# OpenAI / Design generation (SERVER-SIDE ONLY — Supabase Edge Function secrets)
# Do NOT set these in Vite / React / any VITE_ variable.
# Do NOT commit real values.
# See: supabase/functions/.env.example
#
#   supabase secrets set OPENAI_API_KEY=...
#   supabase secrets set DESIGN_GENERATION_MODE=demo
#
# DESIGN_GENERATION_MODE: demo | live (missing/invalid => demo)
# Keep DESIGN_GENERATION_MODE=demo until Live Mode is intentionally enabled.
# OPENAI_API_KEY=
# DESIGN_GENERATION_MODE=demo
# ---------------------------------------------------------------------------
